% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkg_input_fixes.R
\name{bkg_fix_remove}
\alias{bkg_fix_remove}
\title{Remove one or several patterns from an address component}
\usage{
bkg_fix_remove(pattern, ignore.case = FALSE, fixed = FALSE)
}
\arguments{
\item{pattern}{\code{[character]} One or more regex patterns (or plain
strings, if \code{fixed = TRUE}) to remove.}

\item{ignore.case, fixed}{\code{[logical]} Passed through to
\code{\link[base]{gsub}}.}
}
\value{
\code{[function]} A fix usable with
\code{\link{bkg_add_input_fix}}.
}
\description{
Builds a fix (for use with \code{\link{bkg_add_input_fix}})
that removes every occurrence of one or more regex patterns. Multiple
patterns are removed in the order given, all within a single fix --
equivalent to chaining several \code{gsub(pattern, "", x)} calls, but
without writing out a custom function.
}
\examples{
\dontrun{
bkg_add_input_fix(place = bkg_fix_remove(c("^Landkreis ", "^Kreis ")))
}

}
